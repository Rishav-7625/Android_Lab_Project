<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.timetabledemo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/sked"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WeekActivity" />
        <activity android:name=".DayDetail" />
        <activity android:name=".Subject_Day" />
        <activity android:name=".Sub_Res" />
        <activity android:name=".Faculty" />
        <activity android:name=".ArjunSir" />
        <activity android:name=".SirishSir" />
        <activity android:name=".ChethanSir" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".DevActivity"></activity>
    </application>

</manifest>